<h2>投稿の検索結果一覧</h2>

<table class="table table-hover table-inverse">

    <!--検索対象モデルがPostの時 -->
    <tbody>
      <% @posts.each do |post| %>
        <tr>
          <td><%= link_to post.title, post_path(post.id) %></td>
          <td><%= post.text %></td>
        </tr>
      <% end %>
    </tbody>
</table>